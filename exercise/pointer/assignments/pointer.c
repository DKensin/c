#include <stdio.h>

#define MAX_SIZE    100

int main(void)
{
    int n;  /* size array */
    int arr[MAX_SIZE] = {0};
    int index = 0;
    int *ptr = arr; /* pointer to array */

    /* input value to array use ptr instead of arr[i] */

    /* print array value use ptr instead of arr[i] */

    return 0;
}